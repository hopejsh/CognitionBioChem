# DockQ environment (.venvdockq). Separate because DockQ 2.1.3 hard-pins numpy<2 while the
# main environment's scipy/pandas/prodigy stack requires numpy>=2. Installing DockQ into the
# main venv silently downgrades numpy and leaves scipy running outside its supported range.
#   python3 -m venv .venvdockq && ./.venvdockq/bin/pip install -r requirements-dockq.txt
numpy<2
DockQ==2.1.3
